server:
  port: 9091

spring:
    datasource:
      driver-class-name: org.postgresql.Driver
      url: jdbc:postgresql://localhost:5432/AuthDB
      username: postgres
      password: postgres
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
